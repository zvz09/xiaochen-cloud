/*
import http from "@/config/axios";
import { FLOWABLE_SERVICE_PATH } from "@/api/config/servicePort";

/!**
 * 查询流程实例详情信息
 * @param {string} procInsId
 * @param {string} deployId
 * @returns
 *!/
export function detailInstance(procInsId: string, deployId: string): Promise<Detail_1Res> {
  return http.get(FLOWABLE_SERVICE_PATH + `/instance/${procInsId}/${deployId}/detail`);
}
*/
